---
import FormAndViewer from "@/components/Comment/FormAndViewer";
import HeartButton from "@/components/Comment/HeartButton.astro";
import { useTranslations } from "@/i18n/utils";

interface Props {
  id: string;
  lang: string;
}

const { id, lang } = Astro.props;
---

<HeartButton id={id} />
<FormAndViewer lang={lang} postId={id} client:idle />
